请继续完善当前桌面 Web 端企业级自动化测试平台的 Figma 设计。

前两批已经完成：
1. 接口管理辅助弹层
2. 接口场景弹窗、步骤配置和执行结果详情

本轮必须复用前两批已经确认的设计规范，包括：
- 弹窗和抽屉视觉规范
- 抽屉宽度与内容结构
- 固定标题栏和底部操作栏
- 表单布局
- Tab 样式
- Monaco Editor 样式
- 加载、错误、空状态和无权限状态
- 删除确认和未保存确认
- 敏感信息展示规则

本轮只补充：
「Web UI 自动化 / 元素管理 + Web UI 步骤编辑」

不要重新设计以下已经完成的页面：
- Web UI 元素管理主列表
- Web UI 用例主页面
- Web UI 套件主页面
- 浏览器录制页面
- Web UI 执行列表
- 系统导航和整体页面框架

不要参考旧前端的视觉样式。
旧系统截图和代码只用于理解业务功能、字段和流程，不作为视觉参考。

产品背景：
Web UI 元素管理用于集中维护页面元素及其定位器，分析元素质量、验证定位器，并查看元素被哪些 Web UI 用例和步骤引用。

Web UI 步骤编辑用于配置浏览器操作、元素交互、等待、页面切换、文件上传、脚本和断言等自动化步骤。

当前阶段只设计桌面 Web 浏览器自动化，不设计 APP、Android、iOS 或移动端自动化。

前端技术架构：
- 桌面 Web SPA
- Vue 3
- TypeScript
- Element Plus
- Monaco Editor
- Playwright 执行能力
- 自定义业务组件
- CSS Design Tokens

设计需要能够真实落地到：
- el-dialog
- el-drawer
- el-form
- el-tabs
- el-table
- el-tree
- el-select
- el-cascader
- el-input
- el-input-number
- el-switch
- el-radio-group
- el-alert
- el-tag
- el-upload
- el-collapse
- el-steps
- el-timeline
- el-progress
- el-descriptions
- Monaco Editor

页面位置：
Web UI 自动化 > 元素管理
Web UI 自动化 > 用例编辑 / 步骤编辑

本轮设计范围：

一、新增 / 编辑元素抽屉

请设计统一的新增和编辑元素抽屉。

需要包含：
- 元素名称
- 所属页面或模块
- 元素描述
- 元素类型
- 主定位器
- 备用定位器
- 定位器优先级
- 所属 Frame
- Shadow DOM 路径
- 测试环境
- 页面地址
- 元素截图
- 标签
- 备注
- 是否启用

支持的定位方式：
- CSS Selector
- XPath
- ID
- Name
- Text
- Playwright Role
- Label
- Placeholder
- Test ID
- 自定义属性

每条定位器需要包含：
- 定位方式
- 定位值
- 优先级
- 是否启用
- 最近验证结果
- 匹配元素数量
- 复制
- 验证
- 删除
- 拖拽排序

需要设计：
- 默认状态
- 编辑状态
- 新增备用定位器
- 定位值格式错误
- 多个元素匹配
- 没有匹配元素
- 主定位器不可用
- 备用定位器可用
- 元素名称重复
- 页面地址无效
- 保存中
- 保存失败
- 未保存关闭确认

二、定位器在线验证

用户可以在新增或编辑元素时验证定位器。

验证配置需要包含：
- 运行环境
- 页面地址
- 浏览器类型
- 浏览器窗口尺寸
- 是否等待页面加载完成
- 最大等待时间
- 是否使用登录状态
- 开始验证

验证结果需要包含：
- 验证成功或失败
- 页面是否打开成功
- 匹配元素数量
- 实际命中的元素
- 元素文本
- 元素标签
- 元素属性
- 是否可见
- 是否可交互
- 元素在页面中的截图
- 页面截图中的元素高亮
- DOM 片段
- 执行耗时
- Runner 节点
- 错误原因

需要设计：
- 等待验证
- 启动浏览器中
- 页面加载中
- 正在查找元素
- 验证成功
- 没有匹配元素
- 匹配多个元素
- 页面打开失败
- 页面登录失效
- 浏览器不可用
- Runner 节点离线
- 验证超时
- 用户取消验证

三、元素详情抽屉

详情抽屉需要清楚展示：
- 元素基础信息
- 当前状态
- 主定位器
- 备用定位器
- 定位器优先级
- 所属页面
- Frame / Shadow DOM 信息
- 最近验证结果
- 最近验证时间
- 最近修改人
- 元素截图
- 质量评分
- 引用数量
- 最近变更记录

主要操作：
- 编辑元素
- 验证元素
- 查看引用
- 查看质量分析
- 禁用元素
- 删除元素

需要设计：
- 正常元素
- 从未验证
- 定位器失效
- 存在多个匹配
- 低质量元素
- 已禁用
- 无查看权限

四、元素引用关系抽屉

用于查看当前元素在哪里被使用。

需要包含：
- Web UI 用例
- Web UI 套件
- 用例步骤
- 使用次数
- 步骤类型
- 用例状态
- 最近执行结果
- 最近执行时间
- 创建人
- 前往用例或步骤

需要支持：
- 按用例搜索
- 按使用模块筛选
- 只查看最近失败的引用
- 展开查看具体步骤

需要设计：
- 没有引用
- 正常引用
- 引用的用例已删除
- 引用的步骤已禁用
- 数据加载失败
- 无权限查看关联用例

五、元素修改影响分析

用户修改主定位器、Frame、Shadow DOM 或禁用元素前，需要展示影响分析。

需要包含：
- 受影响用例数量
- 受影响步骤数量
- 受影响套件数量
- 最近执行失败数量
- 高风险引用
- 变更前定位器
- 变更后定位器
- 是否需要重新验证
- 影响范围列表

操作包括：
- 返回修改
- 保存但不启用
- 保存并验证
- 确认更新

需要设计：
- 无影响
- 低风险
- 高风险
- 影响分析失败
- 部分引用无权限查看

六、元素质量分析

需要对定位器提供可理解的质量评价。

质量信息可以包含：
- 唯一性
- 稳定性
- 可读性
- 页面匹配情况
- 是否依赖动态 class
- 是否依赖层级过深 XPath
- 是否使用推荐属性
- 最近验证成功率
- 最近失败次数
- 备用定位器完整性

需要展示：
- 综合质量评分
- 风险项
- 优化建议
- 推荐定位器
- 应用推荐定位器
- 保留当前定位器

质量分析要专业、克制，不要做成 BI 大屏。

七、删除和禁用元素

需要设计：
- 删除无引用元素
- 删除存在引用的元素
- 禁用元素
- 批量禁用
- 批量删除
- 部分操作成功
- 无权限操作
- 删除失败

存在引用时不能只显示普通确认弹窗，需要展示具体影响，并阻止直接删除或要求先处理引用关系。

八、Web UI 步骤配置抽屉

这是本轮最重要的设计。

不同步骤类型必须使用统一的抽屉框架，不能每种步骤使用不同布局。

统一结构建议包含：
- 步骤名称
- 步骤类型
- 是否启用
- 基础配置
- 目标元素
- 输入参数
- 等待策略
- 失败处理
- 断言
- 调试结果
- 保存
- 保存并调试
- 取消

请设计以下步骤类型：

1. 页面导航
- 打开 URL
- 刷新页面
- 页面后退
- 页面前进
- 等待页面加载

2. 元素交互
- 点击
- 双击
- 输入文本
- 清空输入框
- 悬停
- 选择下拉项
- 勾选或取消勾选
- 键盘输入
- 滚动到元素

3. 等待步骤
- 固定等待
- 等待元素出现
- 等待元素消失
- 等待元素可点击
- 等待页面 URL
- 等待网络请求
- 等待 JavaScript 条件

4. 页面上下文切换
- 切换浏览器窗口
- 切换 Tab
- 进入 Frame
- 退出 Frame
- 进入 Shadow DOM

5. 文件和脚本
- 上传文件
- 下载文件
- 页面截图
- 元素截图
- 执行 JavaScript
- 设置 Cookie
- 设置 Local Storage / Session Storage

6. 控制步骤
- 条件判断
- 循环
- 变量赋值
- 日志输出
- 引用公共步骤

九、步骤中的元素选择

步骤配置需要支持两种元素来源：

1. 从元素库选择
2. 临时定位器

从元素库选择需要包含：
- 页面或模块目录
- 搜索元素
- 元素状态
- 定位器类型
- 最近验证结果
- 引用次数
- 查看详情
- 选择元素

临时定位器需要包含：
- 定位方式
- 定位值
- Frame
- Shadow DOM
- 在线验证
- 保存到元素库

需要清楚区分：
- 当前步骤引用已有元素
- 当前步骤使用临时定位器
- 当前步骤覆盖了元素库中的定位器

元素库中的元素发生变化时，需要提示当前步骤受到影响。

十、Frame 和 Shadow DOM 配置

请设计清晰的层级配置方式，不要让用户直接填写一整串难以理解的路径。

需要支持：
- 添加 Frame 层级
- 选择 Frame 元素
- 添加 Shadow Root 层级
- 层级排序
- 查看完整定位路径
- 逐层验证
- 删除层级

需要设计：
- Frame 不存在
- 跨域 Frame 无法访问
- Shadow Root 未找到
- 中间层级失效
- 最终元素未匹配
- 层级验证成功

十一、文件上传步骤

需要包含：
- 文件来源
- 本地测试文件
- 平台文件资源
- 变量生成的文件路径
- 是否允许多个文件
- 文件类型限制
- 文件大小限制
- 上传控件对应的元素
- 上传后校验

需要设计：
- 文件未选择
- 文件不存在
- 文件格式不支持
- 文件过大
- 上传控件无法定位
- 上传中
- 上传成功
- 上传失败

不要让 Figma 使用真实本地磁盘绝对路径作为长期配置，应体现平台文件资源或执行任务可访问的文件。

十二、Web UI 断言编辑

请沿用接口场景已经确认的断言设计语言，但使用适合 Web UI 的断言类型。

支持：
- 元素存在
- 元素不存在
- 元素可见
- 元素不可见
- 元素可点击
- 元素文本
- 元素属性
- 输入框值
- 元素数量
- 页面标题
- 页面 URL
- Cookie
- 页面截图对比
- JavaScript 表达式

需要设计：
- 多条断言
- AND / OR
- 实际值预览
- 断言通过
- 断言失败
- 元素未找到
- 页面截图差异
- 基准截图缺失
- 变量无法解析

十三、单步骤调试

用户可以在步骤配置抽屉内调试当前步骤。

调试配置包括：
- 运行环境
- 浏览器
- Runner 节点
- 页面起始地址
- 是否复用登录状态
- 最大执行时间
- 开始调试
- 取消调试

调试结果包含：
- 当前阶段
- 浏览器启动结果
- 页面加载结果
- 元素定位过程
- 使用的定位器
- 是否使用备用定位器
- 实际输入参数
- 执行结果
- 执行前截图
- 执行后截图
- 控制台日志
- 网络错误
- 断言结果
- 执行耗时

需要设计：
- 未调试
- 等待 Runner
- 启动浏览器中
- 页面加载中
- 定位元素中
- 执行中
- 调试成功
- 元素定位失败
- 操作超时
- 页面已关闭
- 浏览器崩溃
- Runner 离线
- 用户取消

十四、步骤排序、复制和删除

需要设计：
- 拖拽排序
- 复制步骤
- 禁用步骤
- 删除步骤
- 批量启用
- 批量禁用
- 批量删除
- 删除包含子步骤的条件或循环
- 排序失败恢复
- 存在未保存修改

十五、通用异常和保护状态

请统一补充：
- 数据加载失败
- 保存失败
- 无操作权限
- 引用元素已经删除
- 元素定位器已经变更
- 运行环境失效
- Runner 节点离线
- 浏览器版本不兼容
- 变量无法解析
- 页面存在未保存修改
- 高风险删除二次确认
- 长任务执行中禁止关闭

设计要求：
- 所有抽屉和弹窗必须放在真实的 Web UI 元素管理或用例编辑页面背景上展示
- 复用前两批已经确认的弹层和状态规范
- 不要复制旧前端视觉
- 不要做成低代码拖拽平台
- 保持企业级后台系统的信息密度
- 长抽屉固定标题栏和底部操作栏，中间内容独立滚动
- 元素选择、步骤配置和调试结果之间的层级必须清楚
- 复杂配置优先使用分组、Tab 或渐进展开，不要把所有字段同时堆在一张表单里
- 定位器、Frame 和 Shadow DOM 必须对普通测试人员可理解
- 错误状态需要展示原因和下一步操作
- 不要只设计正常状态
- 不要设计 APP 自动化
- 不要设计 Web UI 执行详情
- 不要扩展本轮之外的业务模块

交付要求：
请输出桌面 Web 端 1440px 宽度设计稿。

第一轮优先完成以下核心画面：
1. 新增 / 编辑元素抽屉
2. 定位器在线验证过程和结果
3. 元素详情抽屉
4. 元素引用关系抽屉
5. 元素修改影响分析
6. 元素质量分析
7. 点击或输入步骤配置抽屉
8. Frame / Shadow DOM 步骤配置
9. 文件上传步骤配置
10. Web UI 断言编辑
11. 元素库选择和临时定位器
12. 单步骤调试过程和结果
13. 存在引用的元素删除确认
14. 未保存修改确认

请先完成以上核心设计，完成后停止。